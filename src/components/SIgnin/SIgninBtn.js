import React from 'react'
import { Button } from 'semantic-ui-react'

const SigninBtn = ({ onRouteChange }) => {
    return (
        <div>
            <Button 
                positive
            >
            Sign in</Button>
        </div>
    )
}

export default SigninBtn